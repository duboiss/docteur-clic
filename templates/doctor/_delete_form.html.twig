<form method="post" action="{{ path('app_admin_doctor_delete', {'id': user.id}) }}" onsubmit="return confirm('Souhaitez-vous vraiment supprimer ce docteur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
